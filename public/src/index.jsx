import React from "react";
import ReactDOM from "react-dom/client";
import App from "./FarmQuest_App";
import "./styles.css"; // Tailwind or your custom styles

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
